import type { ModuleDef } from '../types'

// Layout: the two rival compute philosophies side by side (GPU left, ASIC right),
// with the advanced packaging that both sit on spanning the bottom.
export const silicon: ModuleDef = {
  id: 'silicon',
  title: 'Silicon: GPUs, ASICs & Packaging',
  tagline: 'What is actually on the die — and how dies, memory and interposers become one package.',
  world: { w: 1000, h: 620 },
  items: [
    {
      id: 'si.gpu', name: 'GPU die', kind: 'container', zone: 'Two philosophies',
      x: 260, y: 200, w: 400, h: 300,
      note: 'The general-purpose answer: thousands of small cores under a flexible scheduler — programmable for anything, dominant in AI.',
    },
    { id: 'si.sm', name: 'Streaming multiprocessor (SM)', kind: 'atom', parent: 'si.gpu', zone: 'Two philosophies', x: 150, y: 125,
      note: 'The GPU’s repeated unit — its own registers, scratchpad and tensor cores, copied 100+ times; kernels are sliced to fill them all.' },
    { id: 'si.tensor', name: 'Tensor core', kind: 'atom', parent: 'si.gpu', zone: 'Two philosophies', x: 265, y: 185,
      note: 'A small matrix-multiply engine inside each SM — nearly all AI FLOPs happen here, in low precision.',
      role: 'does nearly all the AI matrix math inside each SM' },
    { id: 'si.cuda', name: 'CUDA cores', kind: 'atom', parent: 'si.gpu', zone: 'Two philosophies', x: 360, y: 125,
      note: 'The scalar lanes for everything that isn’t a matmul — softmax, norms, glue. Tensor cores lift, CUDA cores plumb.' },
    { id: 'si.shmem', name: 'Shared memory / L1', kind: 'atom', parent: 'si.gpu', zone: 'Two philosophies', x: 150, y: 270,
      note: 'A tiny software-managed scratchpad inside each SM — the fast tiles FlashAttention computes in.' },
    { id: 'si.l2', name: 'L2 cache', kind: 'atom', parent: 'si.gpu', zone: 'Two philosophies', x: 355, y: 270,
      note: 'Tens of megabytes shared by all SMs — the last stop before a request has to cross to HBM.' },

    {
      id: 'si.asic', name: 'AI ASIC (TPU-style)', kind: 'container', zone: 'Two philosophies',
      x: 740, y: 200, w: 400, h: 300,
      note: 'The specialized answer: a chip built for one job — matrix multiplication — trading flexibility for efficiency and cost.',
      role: 'trades flexibility for efficiency by targeting a single workload',
    },
    { id: 'si.systolic', name: 'Systolic array', kind: 'atom', parent: 'si.asic', zone: 'Two philosophies', x: 645, y: 125,
      note: 'A grid of multiply-accumulate units where data marches through in lockstep — matmuls with almost no control logic or instruction fetch.',
      role: 'multiplies matrices with a lockstep grid of multiply-accumulate units' },
    { id: 'si.vector', name: 'Vector unit', kind: 'atom', parent: 'si.asic', zone: 'Two philosophies', x: 840, y: 125,
      note: 'Handles the non-matmul math — softmax, activations, norms — that the systolic array can’t express.' },
    { id: 'si.srambuf', name: 'On-chip SRAM buffers', kind: 'atom', parent: 'si.asic', zone: 'Two philosophies', x: 645, y: 270,
      note: 'Large software-scheduled staging memory that keeps the systolic array fed — the compiler, not hardware, decides what lives here.' },
    { id: 'si.ici', name: 'Inter-chip interconnect', kind: 'atom', parent: 'si.asic', zone: 'Two philosophies', x: 840, y: 270,
      note: 'Direct chip-to-chip links wiring ASICs into a torus — the pod is the computer, no Ethernet in sight.' },

    {
      id: 'si.pkg', name: 'The package', kind: 'container', zone: 'Putting it together',
      x: 500, y: 480, w: 800, h: 200,
      note: 'Where separate dies become one part — advanced packaging is now as much of the performance story as the transistors.',
    },
    { id: 'si.hbm', name: 'HBM stack', kind: 'atom', parent: 'si.pkg', zone: 'Putting it together', x: 200, y: 445,
      note: 'DRAM dies stacked vertically right beside the compute die — the memory that holds the weights and KV cache.',
      role: 'stacks DRAM beside the compute die to hold weights and KV cache' },
    { id: 'si.tsv', name: 'Through-silicon vias', kind: 'atom', parent: 'si.pkg', zone: 'Putting it together', x: 320, y: 520,
      note: 'Vertical wires drilled straight through the stacked DRAM dies — the reason HBM can be a tower instead of a sprawl.' },
    { id: 'si.interposer', name: 'Silicon interposer (CoWoS)', kind: 'atom', parent: 'si.pkg', zone: 'Putting it together', x: 450, y: 445,
      note: 'The silicon slab the compute die and HBM sit on; its thousands of fine wires are what make HBM bandwidth physically possible.',
      role: 'connects the compute die to HBM through thousands of fine wires' },
    { id: 'si.chiplet', name: 'Chiplets', kind: 'atom', parent: 'si.pkg', zone: 'Putting it together', x: 590, y: 520,
      note: 'Split one giant die into several smaller ones stitched together in-package — better yield, and each piece on the process node it needs.' },
    { id: 'si.reticle', name: 'Reticle limit', kind: 'atom', parent: 'si.pkg', zone: 'Putting it together', x: 720, y: 445,
      note: '~830 mm² — the largest die one lithography exposure can print. The wall that forces chiplets and wafer-scale workarounds.' },
    { id: 'si.wafer', name: 'Wafer-scale integration', kind: 'atom', parent: 'si.pkg', zone: 'Putting it together', x: 850, y: 520,
      note: 'Skip dicing entirely and use the whole wafer as one chip — no off-chip hops, at the price of heroic yield and cooling tricks.' },
  ],
}
